import type { JSX } from "react";

const BurrgerButton = (): JSX.Element => {
  return (
    <button className="center-elements fixed top-9.5 right-6 mt-2 size-12 rounded-full bg-white sm:top-12.5 sm:right-12.5 md:size-16"></button>
  );
};

export default BurrgerButton;
